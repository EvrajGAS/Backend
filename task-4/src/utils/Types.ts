export enum ProductStatus{
    ACTIVE = "ACTIVE",
    ARCHIVED = 'ARCHIVED',
    DRAFT = "DRAFT",
    UNLISTED = "UNLISTED"
}

export enum InventoryPolicies{
    CONTINUE = "CONTINUE",
    DENY = "DENY"
}